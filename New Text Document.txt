<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CROWNKICKS</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:ital,wght@0,400;0,500;0,600;0,700;0,900;1,900&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
<style>
/* ═══════════════════════════════════════════════
   TOKENS
═══════════════════════════════════════════════ */
:root{
  --bg:          #0a0a0a;
  --surface:     #111111;
  --surface-2:   #1a1a1a;
  --surface-3:   #242424;
  --border:      rgba(255,255,255,.07);
  --border-md:   rgba(255,255,255,.12);
  --gold:        #C9A227;
  --gold-lt:     #E8C547;
  --gold-dk:     #9E7E1A;
  --gold-glow:   rgba(201,162,39,.18);
  --white:       #ffffff;
  --text:        rgba(255,255,255,.55);
  --text-md:     rgba(255,255,255,.75);
  --red:         #e53935;
  --ff-display:  'Bebas Neue', sans-serif;
  --ff-body:     'Barlow', sans-serif;
  --radius:      6px;
  --radius-lg:   12px;
  --trans:       .25s ease;
  --trans-s:     .4s cubic-bezier(.34,1.56,.64,1);
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{scroll-behavior:smooth}
body{
  font-family:var(--ff-body);
  background:var(--bg);
  color:var(--white);
  overflow-x:hidden;
  -webkit-font-smoothing:antialiased;
}
a{text-decoration:none;color:inherit}
ul{list-style:none}
button{cursor:pointer;border:none;background:none;font-family:inherit}
img{display:block;max-width:100%}

/* ═══════════════════════════════════════════════
   SCROLLBAR
═══════════════════════════════════════════════ */
::-webkit-scrollbar{width:6px}
::-webkit-scrollbar-track{background:var(--bg)}
::-webkit-scrollbar-thumb{background:var(--surface-3);border-radius:3px}
::-webkit-scrollbar-thumb:hover{background:var(--gold-dk)}

/* ═══════════════════════════════════════════════
   NAVBAR
═══════════════════════════════════════════════ */
.navbar{
  background:rgba(10,10,10,.92);
  backdrop-filter:blur(20px);
  -webkit-backdrop-filter:blur(20px);
  padding:0 5%;
  position:sticky;top:0;z-index:300;
  border-bottom:1px solid var(--border);
}
.navbar-top{
  display:flex;justify-content:space-between;align-items:center;
  gap:20px;height:68px;
}
.navbar-logo{
  font-family:var(--ff-display);
  font-size:26px;letter-spacing:.12em;color:var(--white);
  white-space:nowrap;flex-shrink:0;
  transition:color var(--trans);
}
.navbar-logo:hover{color:var(--gold)}
.navbar-logo span{color:var(--gold)}

/* Search */
.navbar-search{
  display:flex;align-items:center;flex:1;max-width:460px;
  background:var(--surface-2);
  border:1px solid var(--border-md);
  border-radius:var(--radius);
  overflow:hidden;
  transition:border-color var(--trans),box-shadow var(--trans);
}
.navbar-search:focus-within{
  border-color:var(--gold);
  box-shadow:0 0 0 3px var(--gold-glow);
}
.navbar-search input{
  flex:1;border:none;background:transparent;
  padding:10px 16px;font-size:14px;font-family:inherit;
  color:var(--white);outline:none;
}
.navbar-search input::placeholder{color:var(--text)}
.navbar-search button{
  padding:10px 16px;background:var(--gold);
  color:var(--bg);font-size:14px;
  transition:background var(--trans);flex-shrink:0;
}
.navbar-search button:hover{background:var(--gold-lt)}

/* Icons */
.navbar-icons{display:flex;align-items:center;gap:22px}
.navbar-icons i{
  font-size:19px;color:var(--text-md);cursor:pointer;
  transition:color var(--trans),transform var(--trans-s);
}
.navbar-icons i:hover{color:var(--gold);transform:scale(1.15)}

.cart-icon-wrapper{position:relative;cursor:pointer}
.cart-count{
  position:absolute;top:-8px;right:-8px;
  background:var(--gold);color:var(--bg);
  font-size:10px;font-weight:800;
  width:18px;height:18px;border-radius:50%;
  display:flex;align-items:center;justify-content:center;
  transition:transform var(--trans-s);
}
.cart-count.pop{animation:badge-pop .35s var(--trans-s)}
@keyframes badge-pop{0%{transform:scale(.5)}60%{transform:scale(1.4)}100%{transform:scale(1)}}

/* Nav links row */
.navbar-links{
  display:flex;justify-content:center;align-items:center;
  gap:4px;height:44px;border-top:1px solid var(--border);
}
.navbar-links a{
  color:var(--text);font-size:14px;font-weight:600;
  padding:8px 18px;letter-spacing:.06em;text-transform:uppercase;
  border-radius:var(--radius);
  transition:color var(--trans),background var(--trans);
  position:relative;
}
.navbar-links a::after{
  content:'';position:absolute;bottom:0;left:50%;
  width:0;height:1.5px;background:var(--gold);
  transform:translateX(-50%);transition:width var(--trans);
}
.navbar-links a:hover,
.navbar-links a.active{color:var(--white)}
.navbar-links a:hover::after,
.navbar-links a.active::after{width:60%}

/* Drawer search — mobile only */
.drawer-search{display:none}

/* Hamburger */
.hamburger{
  display:none;flex-direction:column;gap:5px;
  cursor:pointer;background:none;border:none;padding:4px;
}
.hamburger span{
  display:block;width:22px;height:1.5px;
  background:var(--white);border-radius:2px;transition:all .3s ease;
}
.hamburger.open span:nth-child(1){transform:translateY(6.5px) rotate(45deg)}
.hamburger.open span:nth-child(2){opacity:0}
.hamburger.open span:nth-child(3){transform:translateY(-6.5px) rotate(-45deg)}

/* ═══════════════════════════════════════════════
   CART SIDEBAR
═══════════════════════════════════════════════ */
.cart-overlay{
  position:fixed;inset:0;background:rgba(0,0,0,.75);
  z-index:399;opacity:0;visibility:hidden;
  transition:opacity var(--trans),visibility var(--trans);
  backdrop-filter:blur(4px);
}
.cart-overlay.open{opacity:1;visibility:visible}

.cart-sidebar{
  position:fixed;top:0;right:0;
  width:400px;height:100%;
  background:var(--surface);
  border-left:1px solid var(--border-md);
  z-index:400;
  display:flex;flex-direction:column;
  transform:translateX(100%);
  transition:transform .32s cubic-bezier(.4,0,.2,1);
}
.cart-sidebar.open{transform:translateX(0)}

.cart-header{
  display:flex;justify-content:space-between;align-items:center;
  padding:24px 24px 20px;
  border-bottom:1px solid var(--border);
}
.cart-title{
  font-family:var(--ff-display);font-size:26px;
  letter-spacing:.1em;color:var(--white);
}
.cart-close{
  width:34px;height:34px;border-radius:50%;
  background:var(--surface-2);color:var(--text-md);font-size:16px;
  display:flex;align-items:center;justify-content:center;
  transition:background var(--trans),color var(--trans);
}
.cart-close:hover{background:var(--gold);color:var(--bg)}

.cart-items{
  flex:1;overflow-y:auto;padding:20px 24px;
  display:flex;flex-direction:column;gap:16px;
}
.cart-empty{
  text-align:center;color:var(--text);font-size:14px;
  margin-top:60px;display:flex;flex-direction:column;align-items:center;gap:14px;
}
.cart-empty i{font-size:40px;color:var(--surface-3)}

.cart-item{
  display:flex;gap:14px;align-items:center;
  padding-bottom:16px;border-bottom:1px solid var(--border);
}
.cart-item-img{
  width:72px;height:72px;object-fit:cover;
  border-radius:var(--radius);flex-shrink:0;
  background:var(--surface-2);
}
.cart-item-details{flex:1;display:flex;flex-direction:column;gap:3px}
.cart-item-brand{font-size:11px;font-weight:600;color:var(--text);text-transform:uppercase;letter-spacing:.1em}
.cart-item-name{font-size:14px;font-weight:700;color:var(--white)}
.cart-item-price{font-size:14px;font-weight:700;color:var(--gold)}
.cart-item-remove{
  background:none;border:none;color:var(--text);
  cursor:pointer;font-size:15px;padding:4px;
  transition:color var(--trans);
}
.cart-item-remove:hover{color:var(--red)}

.cart-item-controls{display:flex;align-items:center;gap:8px;margin-top:6px}
.qty-btn{
  width:26px;height:26px;border:1px solid var(--border-md);
  border-radius:4px;background:var(--surface-2);color:var(--text-md);
  font-size:12px;display:flex;align-items:center;justify-content:center;
  transition:all var(--trans);cursor:pointer;
}
.qty-btn:hover{background:var(--gold);color:var(--bg);border-color:var(--gold)}
.qty-number{font-size:14px;font-weight:700;min-width:18px;text-align:center}

.cart-footer{
  padding:20px 24px;border-top:1px solid var(--border);
  display:flex;flex-direction:column;gap:16px;
}
.cart-total{
  display:flex;justify-content:space-between;align-items:center;
  font-size:16px;font-weight:700;
}
#cart-total-price{color:var(--gold);font-size:20px}
.checkout-btn{
  width:100%;padding:15px;
  background:var(--gold);color:var(--bg);
  font-family:inherit;font-size:14px;font-weight:800;
  letter-spacing:.12em;text-transform:uppercase;
  border-radius:var(--radius);
  transition:background var(--trans),transform var(--trans-s);
}
.checkout-btn:hover{background:var(--gold-lt);transform:translateY(-1px)}
@media(max-width:480px){.cart-sidebar{width:100%}}

/* ═══════════════════════════════════════════════
   HERO
═══════════════════════════════════════════════ */
.hero{
  position:relative;min-height:92vh;
  display:flex;align-items:center;
  overflow:hidden;
  background:var(--bg);
}

/* Radial light behind shoe */
.hero::before{
  content:'';position:absolute;
  right:5%;top:50%;transform:translateY(-50%);
  width:700px;height:700px;border-radius:50%;
  background:radial-gradient(ellipse at center,
    rgba(201,162,39,.12) 0%,
    rgba(201,162,39,.04) 40%,
    transparent 70%);
  pointer-events:none;animation:pulse-bg 4s ease-in-out infinite;
}
@keyframes pulse-bg{0%,100%{opacity:.8;transform:translateY(-50%) scale(1)}50%{opacity:1;transform:translateY(-50%) scale(1.06)}}

/* Grid texture */
.hero-grid{
  position:absolute;inset:0;pointer-events:none;
  background-image:
    linear-gradient(rgba(201,162,39,.035) 1px,transparent 1px),
    linear-gradient(90deg,rgba(201,162,39,.035) 1px,transparent 1px);
  background-size:72px 72px;
  mask-image:radial-gradient(ellipse 80% 80% at 50% 50%,black 40%,transparent 100%);
}

/* Scanline noise */
.hero-noise{
  position:absolute;inset:0;pointer-events:none;opacity:.04;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  background-size:200px;
}

/* Left content */
.hero-left{
  position:relative;z-index:2;
  padding:0 5%;width:55%;
  animation:slide-up .9s cubic-bezier(.16,1,.3,1) forwards;opacity:0;
}
@keyframes slide-up{from{opacity:0;transform:translateY(36px)}to{opacity:1;transform:translateY(0)}}

.hero-eyebrow{
  display:inline-flex;align-items:center;gap:10px;
  font-size:11px;font-weight:700;letter-spacing:.25em;text-transform:uppercase;
  color:var(--gold);margin-bottom:20px;
}
.hero-eyebrow::before{content:'';display:block;width:32px;height:1.5px;background:var(--gold)}

.hero-title{
  font-family:var(--ff-display);
  font-size:clamp(52px,8.5vw,110px);
  line-height:.92;letter-spacing:.03em;
  color:var(--white);margin-bottom:12px;
}
.hero-title .gold{color:var(--gold)}
.hero-title .stroke{
  -webkit-text-stroke:1.5px var(--white);
  color:transparent;
}

.hero-sub{
  font-size:clamp(15px,1.6vw,18px);
  color:var(--text-md);line-height:1.75;
  max-width:460px;margin-bottom:40px;
  font-weight:500;
}

.hero-ctas{display:flex;gap:14px;flex-wrap:wrap;align-items:center}
.btn-primary{
  display:inline-flex;align-items:center;gap:10px;
  padding:15px 32px;
  background:var(--gold);color:var(--bg);
  font-size:13px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;
  border-radius:var(--radius);
  transition:background var(--trans),transform var(--trans-s),box-shadow var(--trans);
}
.btn-primary:hover{
  background:var(--gold-lt);transform:translateY(-2px);
  box-shadow:0 8px 28px rgba(201,162,39,.35);
}
.btn-outline{
  display:inline-flex;align-items:center;gap:10px;
  padding:14px 28px;
  border:1.5px solid var(--border-md);color:var(--text-md);
  font-size:13px;font-weight:700;letter-spacing:.1em;text-transform:uppercase;
  border-radius:var(--radius);
  transition:border-color var(--trans),color var(--trans),transform var(--trans-s);
}
.btn-outline:hover{border-color:var(--gold);color:var(--gold);transform:translateY(-2px)}

/* Stats row */
.hero-stats{
  display:flex;gap:36px;margin-top:56px;
  padding-top:36px;border-top:1px solid var(--border);
  flex-wrap:wrap;
}
.hstat-num{
  font-family:var(--ff-display);font-size:36px;
  letter-spacing:.04em;color:var(--gold);display:block;
}
.hstat-label{font-size:12px;color:var(--text);font-weight:600;letter-spacing:.1em;text-transform:uppercase}

/* Floating shoe */
.hero-right{
  position:absolute;right:3%;top:50%;transform:translateY(-50%);
  width:48%;max-width:720px;z-index:2;
  animation:float-in .9s .15s cubic-bezier(.16,1,.3,1) both;
}
@keyframes float-in{from{opacity:0;transform:translateY(-40%) translateX(60px)}to{opacity:1;transform:translateY(-50%) translateX(0)}}

.shoe-wrap{
  position:relative;
  animation:shoe-float 4s ease-in-out infinite;
  filter:drop-shadow(0 40px 80px rgba(201,162,39,.25));
}
@keyframes shoe-float{
  0%,100%{transform:translateY(0) rotate(-2deg)}
  50%{transform:translateY(-22px) rotate(-4deg)}
}
.shoe-img{width:100%;object-fit:contain}

/* Glow ring under shoe */
.shoe-glow{
  position:absolute;bottom:-4%;left:50%;transform:translateX(-50%);
  width:60%;height:24px;
  background:radial-gradient(ellipse at center,rgba(201,162,39,.5) 0%,transparent 70%);
  filter:blur(10px);
  animation:shoe-float 4s ease-in-out infinite;
}

/* Floating tags around shoe */
.float-tag{
  position:absolute;
  background:var(--surface);border:1px solid var(--border-md);
  border-radius:10px;padding:10px 14px;
  display:flex;align-items:center;gap:10px;
  backdrop-filter:blur(16px);
  box-shadow:0 8px 32px rgba(0,0,0,.4);
  animation:tag-bob 4s ease-in-out infinite;
}
.float-tag-icon{font-size:20px;flex-shrink:0}
.float-tag-text{display:flex;flex-direction:column}
.float-tag-title{font-size:12px;font-weight:800;color:var(--white);line-height:1.1}
.float-tag-sub{font-size:10px;color:var(--text);letter-spacing:.06em}

.tag-1{top:10%;left:-10%;animation-delay:.5s}
.tag-2{bottom:18%;right:-8%;animation-delay:1.2s}
.tag-3{top:55%;left:-5%;animation-delay:.8s;animation-duration:3.2s}

@keyframes tag-bob{
  0%,100%{transform:translateY(0)}
  50%{transform:translateY(-8px)}
}

/* Quick actions strip */
.quick-strip{
  display:flex;gap:20px;
  padding:18px 5%;
  background:var(--surface);
  border-bottom:1px solid var(--border);
  overflow-x:auto;
  scrollbar-width:none;
}
.quick-strip::-webkit-scrollbar{display:none}
.qchip{
  display:flex;align-items:center;gap:8px;
  padding:9px 18px;
  background:var(--surface-2);
  border:1px solid var(--border-md);
  border-radius:var(--radius-full, 999px);
  font-size:13px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;
  color:var(--text-md);cursor:pointer;white-space:nowrap;flex-shrink:0;
  transition:all var(--trans);
}
.qchip i{font-size:14px;color:var(--gold)}
.qchip:hover,.qchip.active{
  background:var(--gold);color:var(--bg);border-color:var(--gold);
}
.qchip:hover i,.qchip.active i{color:var(--bg)}

/* ═══════════════════════════════════════════════
   SHARED SECTION STYLES
═══════════════════════════════════════════════ */
.section-title{
  font-family:var(--ff-display);
  font-size:clamp(28px,4vw,42px);
  letter-spacing:.06em;color:var(--white);
  text-transform:uppercase;margin-bottom:24px;
}
.section-title span{color:var(--gold)}

/* ═══════════════════════════════════════════════
   CATEGORY GRID
═══════════════════════════════════════════════ */
.main{padding:5% 5%}

.category-grid{
  display:grid;
  gap:6px;
  grid-template-columns:1.6fr 1fr 1fr;
  grid-template-rows:260px 260px;
}
.card{
  position:relative;overflow:hidden;
  border-radius:var(--radius);cursor:pointer;
  background:var(--surface-2);
}
.card img{
  width:100%;height:100%;object-fit:cover;display:block;
  transition:transform .55s ease;
}
.card:hover img{transform:scale(1.07)}
.card::after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(to top,rgba(0,0,0,.78) 0%,rgba(0,0,0,.1) 55%,transparent 100%);
  pointer-events:none;
}
.card-label{
  position:absolute;bottom:20px;left:22px;z-index:2;
  font-family:var(--ff-display);
  font-size:clamp(22px,3vw,36px);letter-spacing:.06em;
  color:var(--white);text-transform:uppercase;
  text-shadow:0 2px 10px rgba(0,0,0,.5);
}
.card-sneakers{grid-column:1;grid-row:1/3}
.card-running {grid-column:2;grid-row:1/3}
.card-casual  {grid-column:3;grid-row:1}
.card-limited {grid-column:3;grid-row:2;background:#0d0d0d}
.card-limited::after{display:none}
.limited-body{
  position:absolute;inset:0;
  display:flex;flex-direction:column;align-items:center;justify-content:center;
  gap:10px;padding:20px;text-align:center;
}
.limited-body::before{content:'';display:block;width:36px;height:2px;background:var(--gold)}
.limited-title{
  font-family:var(--ff-display);
  font-size:clamp(18px,2.5vw,26px);
  letter-spacing:.2em;text-transform:uppercase;
  color:var(--gold);text-shadow:0 0 30px rgba(201,162,39,.4);
}
.limited-sub{
  font-size:12px;font-weight:600;letter-spacing:.3em;
  color:rgba(255,255,255,.5);text-transform:uppercase;
}

/* ═══════════════════════════════════════════════
   NEW ARRIVALS
═══════════════════════════════════════════════ */
.arrivals{padding:2% 5% 5%}
.arrivals-header{
  display:flex;justify-content:space-between;align-items:center;
  margin-bottom:32px;
}
.view-all{
  color:var(--gold);font-size:13px;font-weight:700;
  letter-spacing:.1em;text-transform:uppercase;
  display:flex;align-items:center;gap:8px;
  transition:gap var(--trans);
}
.view-all:hover{gap:14px}

.products-grid{
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:18px;
}
.product-card{
  background:var(--surface);
  border:1px solid var(--border);
  border-radius:var(--radius-lg);
  overflow:hidden;cursor:pointer;
  transition:transform var(--trans),box-shadow var(--trans),border-color var(--trans);
}
.product-card:hover{
  transform:translateY(-6px);
  box-shadow:0 20px 50px rgba(0,0,0,.4);
  border-color:var(--border-md);
}
.product-img{width:100%;aspect-ratio:1/1;overflow:hidden;background:var(--surface-2)}
.product-img img{
  width:100%;height:100%;object-fit:cover;
  transition:transform .55s ease;
}
.product-card:hover .product-img img{transform:scale(1.06)}
.product-info{
  padding:14px 16px 18px;
  display:flex;flex-direction:column;gap:6px;
}
.product-brand{
  font-size:11px;font-weight:700;color:var(--text);
  text-transform:uppercase;letter-spacing:.1em;
}
.product-name{font-size:16px;font-weight:700;color:var(--white)}
.product-price{font-size:16px;font-weight:800;color:var(--gold)}
.add-to-cart{
  width:100%;padding:11px;margin-top:4px;
  background:var(--surface-2);color:var(--white);
  border:1px solid var(--border-md);
  border-radius:var(--radius);
  font-family:inherit;font-size:12px;font-weight:800;
  letter-spacing:.12em;text-transform:uppercase;
  transition:background var(--trans),border-color var(--trans),color var(--trans);
}
.add-to-cart:hover{background:var(--gold);border-color:var(--gold);color:var(--bg)}

/* ═══════════════════════════════════════════════
   MANIFESTO
═══════════════════════════════════════════════ */
.manifesto{
  background:linear-gradient(135deg,#0d0d0d 0%,#111 50%,#0d0d0d 100%);
  padding:100px 5%;text-align:center;
  display:flex;flex-direction:column;align-items:center;gap:28px;
  border-top:1px solid var(--border);border-bottom:1px solid var(--border);
  position:relative;overflow:hidden;
}
.manifesto::before{
  content:'CROWN';
  position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);
  font-family:var(--ff-display);font-size:clamp(120px,18vw,260px);
  color:rgba(201,162,39,.04);letter-spacing:.1em;
  pointer-events:none;white-space:nowrap;
}
.manifesto-label{
  font-size:11px;font-weight:700;letter-spacing:.28em;
  text-transform:uppercase;color:var(--gold);
}
.manifesto-text{
  font-family:var(--ff-display);
  font-size:clamp(28px,5vw,60px);
  color:var(--white);text-transform:uppercase;line-height:1.1;
  max-width:900px;position:relative;
}
.manifesto-text span{color:var(--gold);font-style:italic}
.manifesto-line{width:56px;height:2px;background:var(--gold)}

/* ═══════════════════════════════════════════════
   FEATURED
═══════════════════════════════════════════════ */
.featured{padding:5%}
.featured-header{margin-bottom:28px}
.featured-subtitle{font-size:14px;color:var(--text);margin-top:6px;font-weight:500}
.featured-grid{
  display:grid;grid-template-columns:1fr 1fr;gap:6px;
}
.featured-card{
  position:relative;overflow:hidden;
  border-radius:var(--radius);cursor:pointer;height:520px;
  background:var(--surface-2);
}
.featured-card img{
  width:100%;height:100%;object-fit:cover;display:block;
  transition:transform .6s ease;
}
.featured-card:hover img{transform:scale(1.05)}
.featured-card::after{
  content:'';position:absolute;inset:0;
  background:linear-gradient(to top,rgba(0,0,0,.88) 0%,rgba(0,0,0,.35) 50%,transparent 100%);
  pointer-events:none;
}
.featured-overlay{
  position:absolute;bottom:0;left:0;right:0;
  padding:32px;z-index:2;
  display:flex;flex-direction:column;gap:8px;
}
.featured-tag{
  display:inline-block;align-self:flex-start;
  background:var(--gold);color:var(--bg);
  font-size:10px;font-weight:800;letter-spacing:.15em;
  text-transform:uppercase;padding:4px 10px;
  border-radius:3px;margin-bottom:4px;
}
.featured-brand{
  font-size:12px;font-weight:600;color:rgba(255,255,255,.55);
  text-transform:uppercase;letter-spacing:.18em;
}
.featured-name{
  font-family:var(--ff-display);
  font-size:clamp(24px,2.5vw,36px);
  color:var(--white);text-transform:uppercase;line-height:1.05;
}
.featured-price{font-size:22px;font-weight:800;color:var(--gold)}
.featured-btn{
  display:inline-flex;align-items:center;gap:10px;align-self:flex-start;
  color:var(--white);font-size:12px;font-weight:800;
  letter-spacing:.12em;text-transform:uppercase;
  border:1.5px solid rgba(255,255,255,.35);
  padding:10px 20px;border-radius:var(--radius);margin-top:8px;
  transition:all var(--trans);
}
.featured-btn:hover{background:var(--gold);border-color:var(--gold);color:var(--bg)}

/* ═══════════════════════════════════════════════
   BRANDS STRIP
═══════════════════════════════════════════════ */
.brands-strip{
  background:var(--surface);padding:20px 0;
  overflow:hidden;
  border-top:1px solid var(--border);
  border-bottom:1px solid var(--border);
}
.brands-track{
  display:flex;width:max-content;
  animation:scroll-brands 22s linear infinite;
}
.brands-track:hover{animation-play-state:paused}
.brands-list{
  display:flex;align-items:center;gap:48px;
  padding:0 24px;list-style:none;flex-shrink:0;
}
.brands-list li{
  display:flex;align-items:center;gap:10px;
  font-size:14px;font-weight:800;letter-spacing:.2em;
  text-transform:uppercase;color:rgba(255,255,255,.3);
  white-space:nowrap;cursor:default;transition:color var(--trans);
}
.brands-list li:hover{color:var(--white)}
.brands-list li i{font-size:8px;color:var(--gold)}
@keyframes scroll-brands{0%{transform:translateX(0)}100%{transform:translateX(-50%)}}

/* ═══════════════════════════════════════════════
   FOOTER
═══════════════════════════════════════════════ */
.footer{
  padding:5% 5% 100px;
  background:#000;
  display:grid;grid-template-columns:1.5fr 1fr 1fr;
  gap:40px;align-items:start;
  border-top:1px solid var(--border);
}
.footer-logo{
  font-family:var(--ff-display);font-size:44px;
  letter-spacing:.1em;color:var(--gold);
  display:block;margin-bottom:16px;
  transition:opacity var(--trans);
}
.footer-logo:hover{opacity:.8}
.footer .text p{
  font-size:15px;color:var(--text);line-height:1.8;
  max-width:320px;margin-bottom:20px;
}
.footer .text span{color:var(--gold);font-size:13px;font-style:italic}
.footer-col-title{
  font-size:12px;font-weight:700;letter-spacing:.18em;
  text-transform:uppercase;color:var(--white);
  margin-bottom:16px;display:block;
}
.footer-links{display:flex;flex-direction:column;gap:10px}
.footer-links a,.footer .social-media a{
  color:var(--text);font-size:14px;
  transition:color var(--trans),padding-left var(--trans);
}
.footer-links a:hover,.footer .social-media a:hover{color:var(--gold);padding-left:4px}
.footer .social-media{display:flex;flex-direction:column;gap:10px}

/* ═══════════════════════════════════════════════
   RESPONSIVE
═══════════════════════════════════════════════ */
@media(max-width:1024px){
  .hero-right{width:52%}
  .hero-left{width:52%}
  .float-tag{display:none}
}
@media(max-width:860px){
  .hero{flex-direction:column;min-height:auto;padding:60px 0 40px}
  .hero::before{display:none}
  .hero-left{width:100%;text-align:center;padding:0 5%}
  .hero-sub{max-width:100%}
  .hero-ctas{justify-content:center}
  .hero-stats{justify-content:center}
  .hero-right{
    position:relative;top:auto;right:auto;transform:none;
    width:80%;max-width:400px;margin:32px auto 0;
    animation:shoe-float 4s ease-in-out infinite;
  }
  .hero::before,.hero-noise{display:none}
  .products-grid{grid-template-columns:repeat(2,1fr)}
  .category-grid{
    grid-template-columns:1fr 1fr;
    grid-template-rows:220px 220px 180px;
  }
  .card-sneakers{grid-column:1;grid-row:1/3}
  .card-running {grid-column:2;grid-row:1/3}
  .card-casual  {grid-column:1;grid-row:3}
  .card-limited {grid-column:2;grid-row:3}
  .featured-grid{grid-template-columns:1fr 1fr}
  .featured-card{height:380px}
  .footer{grid-template-columns:1fr 1fr;gap:28px}
  .footer .text{grid-column:1/-1}
}
@media(max-width:768px){
  .navbar-top .navbar-search{display:none}
  .hamburger{display:flex}
  .navbar-links{
    display:none;flex-direction:column;align-items:flex-start;
    gap:0;padding-top:0;border-top:1px solid var(--border);margin-top:0;
    height:auto;background:var(--surface);
    position:absolute;top:100%;left:0;right:0;
    padding:12px 5%;z-index:200;
  }
  .navbar-links.open{display:flex}
  .drawer-search{
    display:flex;width:100%;
    background:var(--surface-2);border:1px solid var(--border-md);
    border-radius:var(--radius);overflow:hidden;
    margin:10px 0;
  }
  .drawer-search input{
    flex:1;border:none;background:transparent;
    padding:10px 14px;font-size:14px;font-family:inherit;
    color:var(--white);outline:none;
  }
  .drawer-search input::placeholder{color:var(--text)}
  .drawer-search button{
    padding:10px 16px;background:var(--gold);color:var(--bg);border:none;cursor:pointer;
  }
  .navbar-links a{
    width:100%;padding:13px 4px;
    border-bottom:1px solid var(--border);font-size:15px;
  }
  .navbar-links a::after{display:none}
}
@media(max-width:600px){
  .products-grid{grid-template-columns:repeat(2,1fr)}
  .quick-strip{gap:10px;padding:14px 4%}
  .hero-title{font-size:clamp(44px,14vw,80px)}
}
@media(max-width:480px){
  .products-grid{grid-template-columns:1fr}
  .category-grid{
    grid-template-columns:1fr;
    grid-template-rows:240px 220px 180px 150px;
  }
  .card-sneakers{grid-column:1;grid-row:1}
  .card-running {grid-column:1;grid-row:2}
  .card-casual  {grid-column:1;grid-row:3}
  .card-limited {grid-column:1;grid-row:4}
  .featured-grid{grid-template-columns:1fr}
  .featured-card{height:420px}
  .footer{grid-template-columns:1fr}
  .footer .text{grid-column:1}
  .hero-right{width:95%}
}
</style>
</head>
<body>

<!-- ══════════════════════════════════════
     NAVBAR
══════════════════════════════════════ -->
<nav class="navbar">
  <div class="navbar-top">
    <a href="#" class="navbar-logo">CROWN<span>KICKS</span></a>

    <div class="navbar-search">
      <input type="text" id="searchInput" placeholder="Search sneakers, brands...">
      <button aria-label="Search"><i class="fas fa-search"></i></button>
    </div>

    <div class="navbar-icons">
      <div class="cart-icon-wrapper" id="cartIconWrapper">
        <i class="fa-solid fa-cart-shopping"></i>
        <span class="cart-count" id="cart-count">0</span>
      </div>
      <i class="fa-solid fa-heart" title="Wishlist"></i>
      <i class="fa-solid fa-user" title="Account"></i>
    </div>

    <button class="hamburger" id="hamburger" aria-label="Toggle menu">
      <span></span><span></span><span></span>
    </button>
  </div>

  <div class="navbar-links" id="navbar-links">
    <div class="drawer-search">
      <input type="text" placeholder="Search sneakers, brands...">
      <button><i class="fas fa-search"></i></button>
    </div>
    <a href="#" class="active">Home</a>
    <a href="#">Products</a>
    <a href="#">Limited Edition</a>
    <a href="#">Contact</a>
  </div>
</nav>

<!-- ══════════════════════════════════════
     CART SIDEBAR
══════════════════════════════════════ -->
<div class="cart-overlay" id="cart-overlay"></div>
<div class="cart-sidebar" id="cart-sidebar">
  <div class="cart-header">
    <h2 class="cart-title">Your Cart</h2>
    <button class="cart-close" id="cart-close">
      <i class="fa-solid fa-xmark"></i>
    </button>
  </div>
  <div class="cart-items" id="cart-items"></div>
  <div class="cart-footer">
    <div class="cart-total">
      <span>Total</span>
      <span id="cart-total-price">$0</span>
    </div>
    <button class="checkout-btn">Checkout →</button>
  </div>
</div>

<!-- ══════════════════════════════════════
     HERO
══════════════════════════════════════ -->
<section class="hero">
  <div class="hero-grid"></div>
  <div class="hero-noise"></div>

  <!-- Left copy -->
  <div class="hero-left">
    <span class="hero-eyebrow">New Collection 2025</span>
    <h1 class="hero-title">
      Step Into<br>
      <span class="gold">CROWN</span><br>
      <span class="stroke">KICKS</span>
    </h1>
    <p class="hero-sub">
      Discover the latest trends in footwear and elevate your style with our exclusive collection of sneakers, boots, and more.
    </p>
    <div class="hero-ctas">
      <a href="#" class="btn-primary">Shop Now <i class="fa-solid fa-arrow-right-long"></i></a>
      <a href="#" class="btn-outline">Limited Drops</a>
    </div>
    <div class="hero-stats">
      <div>
        <span class="hstat-num">50K+</span>
        <span class="hstat-label">Pairs Sold</span>
      </div>
      <div>
        <span class="hstat-num">4.8★</span>
        <span class="hstat-label">Avg Rating</span>
      </div>
      <div>
        <span class="hstat-num">200+</span>
        <span class="hstat-label">Styles</span>
      </div>
    </div>
  </div>

  <!-- Floating shoe -->
  <div class="hero-right">
    <div class="shoe-wrap">
      <img class="shoe-img"
           src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?w=900&q=90"
           alt="CrownKicks featured sneaker">
      <div class="shoe-glow"></div>
    </div>

    <!-- Floating info tags -->
    <div class="float-tag tag-1">
      <span class="float-tag-icon">👑</span>
      <div class="float-tag-text">
        <span class="float-tag-title">Best Seller</span>
        <span class="float-tag-sub">Crown Apex 1</span>
      </div>
    </div>
    <div class="float-tag tag-2">
      <span class="float-tag-icon">⚡</span>
      <div class="float-tag-text">
        <span class="float-tag-title">New Drop</span>
        <span class="float-tag-sub">Limited Stock</span>
      </div>
    </div>
    <div class="float-tag tag-3">
      <span class="float-tag-icon">🔥</span>
      <div class="float-tag-text">
        <span class="float-tag-title">$189</span>
        <span class="float-tag-sub">Free Shipping</span>
      </div>
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════
     QUICK ACTIONS STRIP
══════════════════════════════════════ -->
<div class="quick-strip">
  <div class="qchip active"><i class="fa-solid fa-fire"></i> All</div>
  <div class="qchip"><i class="fa-solid fa-shoe-prints"></i> Sneakers</div>
  <div class="qchip"><i class="fa-solid fa-bolt"></i> Running</div>
  <div class="qchip"><i class="fa-solid fa-star"></i> Limited</div>
  <div class="qchip"><i class="fa-solid fa-tag"></i> On Sale</div>
  <div class="qchip"><i class="fa-solid fa-crown"></i> Staff Picks</div>
  <div class="qchip"><i class="fa-regular fa-clock"></i> New Arrivals</div>
  <div class="qchip"><i class="fa-solid fa-trophy"></i> Best Sellers</div>
</div>

<!-- ══════════════════════════════════════
     CATEGORY GRID
══════════════════════════════════════ -->
<main class="main">
  <h2 class="section-title">Shop by <span>Category</span></h2>

  <div class="category-grid">
    <div class="card card-sneakers">
      <img src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?w=800&q=80" alt="Sneakers">
      <span class="card-label">Sneakers</span>
    </div>
    <div class="card card-running">
      <img src="https://images.unsplash.com/photo-1608231387042-66d1773070a5?w=600&q=80" alt="Running">
      <span class="card-label">Running</span>
    </div>
    <div class="card card-casual">
      <img src="https://images.unsplash.com/photo-1525966222134-fcfa99b8ae77?w=600&q=80" alt="Casual">
      <span class="card-label">Casual</span>
    </div>
    <div class="card card-limited">
      <div class="limited-body">
        <span class="limited-title">Limited Edition</span>
        <span class="limited-sub">Archive Drops</span>
      </div>
    </div>
  </div>
</main>

<!-- ══════════════════════════════════════
     NEW ARRIVALS
══════════════════════════════════════ -->
<section class="arrivals">
  <div class="arrivals-header">
    <h2 class="section-title">New <span>Arrivals</span></h2>
    <a href="#" class="view-all">View All <i class="fa-solid fa-arrow-right-long"></i></a>
  </div>

  <div class="products-grid" id="products-grid">
    <!-- CARD 1 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1542291026-7eec264c27ff?w=600&q=80" alt="Nike Air Max 97"></div>
      <div class="product-info">
        <span class="product-brand">Nike</span>
        <h3 class="product-name">Air Max 97</h3>
        <span class="product-price">$189</span>
        <button class="add-to-cart" data-name="Air Max 97" data-brand="Nike" data-price="189" data-img="https://images.unsplash.com/photo-1542291026-7eec264c27ff?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 2 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1608231387042-66d1773070a5?w=600&q=80" alt="Adidas Forum"></div>
      <div class="product-info">
        <span class="product-brand">Adidas</span>
        <h3 class="product-name">Forum Low</h3>
        <span class="product-price">$149</span>
        <button class="add-to-cart" data-name="Forum Low" data-brand="Adidas" data-price="149" data-img="https://images.unsplash.com/photo-1608231387042-66d1773070a5?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 3 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1525966222134-fcfa99b8ae77?w=600&q=80" alt="Puma Suede"></div>
      <div class="product-info">
        <span class="product-brand">Puma</span>
        <h3 class="product-name">Suede Classic</h3>
        <span class="product-price">$99</span>
        <button class="add-to-cart" data-name="Suede Classic" data-brand="Puma" data-price="99" data-img="https://images.unsplash.com/photo-1525966222134-fcfa99b8ae77?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 4 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1606107557195-0e29a4b5b4aa?w=600&q=80" alt="Jordan 1"></div>
      <div class="product-info">
        <span class="product-brand">Jordan</span>
        <h3 class="product-name">Retro High OG</h3>
        <span class="product-price">$219</span>
        <button class="add-to-cart" data-name="Retro High OG" data-brand="Jordan" data-price="219" data-img="https://images.unsplash.com/photo-1606107557195-0e29a4b5b4aa?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 5 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1595950653106-6c9ebd614d3a?w=600&q=80" alt="New Balance 550"></div>
      <div class="product-info">
        <span class="product-brand">New Balance</span>
        <h3 class="product-name">550 White</h3>
        <span class="product-price">$129</span>
        <button class="add-to-cart" data-name="550 White" data-brand="New Balance" data-price="129" data-img="https://images.unsplash.com/photo-1595950653106-6c9ebd614d3a?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 6 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1600185365926-3a2ce3cdb9eb?w=600&q=80" alt="Nike Dunk Low"></div>
      <div class="product-info">
        <span class="product-brand">Nike</span>
        <h3 class="product-name">Dunk Low</h3>
        <span class="product-price">$169</span>
        <button class="add-to-cart" data-name="Dunk Low" data-brand="Nike" data-price="169" data-img="https://images.unsplash.com/photo-1600185365926-3a2ce3cdb9eb?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 7 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1584735175315-9d5df23be620?w=600&q=80" alt="Adidas Ultraboost"></div>
      <div class="product-info">
        <span class="product-brand">Adidas</span>
        <h3 class="product-name">Ultraboost 22</h3>
        <span class="product-price">$199</span>
        <button class="add-to-cart" data-name="Ultraboost 22" data-brand="Adidas" data-price="199" data-img="https://images.unsplash.com/photo-1584735175315-9d5df23be620?w=600&q=80">Add to Cart</button>
      </div>
    </div>
    <!-- CARD 8 -->
    <div class="product-card">
      <div class="product-img"><img src="https://images.unsplash.com/photo-1512374382149-233c42b6a83b?w=600&q=80" alt="Vans Old Skool"></div>
      <div class="product-info">
        <span class="product-brand">Vans</span>
        <h3 class="product-name">Old Skool</h3>
        <span class="product-price">$89</span>
        <button class="add-to-cart" data-name="Old Skool" data-brand="Vans" data-price="89" data-img="https://images.unsplash.com/photo-1512374382149-233c42b6a83b?w=600&q=80">Add to Cart</button>
      </div>
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════
     MANIFESTO
══════════════════════════════════════ -->
<section class="manifesto">
  <span class="manifesto-label">The Manifesto</span>
  <h2 class="manifesto-text">
    CrownKicks is more than footwear.<br>
    It's a statement of <span>Confidence,</span><br>
    Style, and Power.
  </h2>
  <div class="manifesto-line"></div>
</section>

<!-- ══════════════════════════════════════
     FEATURED DROPS
══════════════════════════════════════ -->
<section class="featured">
  <div class="featured-header">
    <h2 class="section-title">Featured <span>Drops</span></h2>
    <p class="featured-subtitle">Handpicked by our team. Limited stock.</p>
  </div>
  <div class="featured-grid">
    <div class="featured-card">
      <img src="https://images.unsplash.com/photo-1600185365926-3a2ce3cdb9eb?w=800&q=80" alt="Nike Dunk Low">
      <div class="featured-overlay">
        <span class="featured-tag">Staff Pick</span>
        <span class="featured-brand">Nike</span>
        <h3 class="featured-name">Dunk Low Retro</h3>
        <span class="featured-price">$169</span>
        <a href="#" class="featured-btn">Shop Now <i class="fa-solid fa-arrow-right-long"></i></a>
      </div>
    </div>
    <div class="featured-card">
      <img src="https://images.unsplash.com/photo-1606107557195-0e29a4b5b4aa?w=800&q=80" alt="Jordan 1 Retro">
      <div class="featured-overlay">
        <span class="featured-tag">Featured Drop</span>
        <span class="featured-brand">Jordan</span>
        <h3 class="featured-name">Retro High OG</h3>
        <span class="featured-price">$219</span>
        <a href="#" class="featured-btn">Shop Now <i class="fa-solid fa-arrow-right-long"></i></a>
      </div>
    </div>
  </div>
</section>

<!-- ══════════════════════════════════════
     BRANDS STRIP
══════════════════════════════════════ -->
<div class="brands-strip">
  <div class="brands-track">
    <ul class="brands-list">
      <li>Nike <i class="fa-solid fa-crown"></i></li>
      <li>Adidas <i class="fa-solid fa-crown"></i></li>
      <li>Jordan <i class="fa-solid fa-crown"></i></li>
      <li>Puma <i class="fa-solid fa-crown"></i></li>
      <li>New Balance <i class="fa-solid fa-crown"></i></li>
      <li>Vans <i class="fa-solid fa-crown"></i></li>
      <li>Reebok <i class="fa-solid fa-crown"></i></li>
      <li>Converse <i class="fa-solid fa-crown"></i></li>
      <li>Limited <i class="fa-solid fa-crown"></i></li>
      <li>Edition <i class="fa-solid fa-crown"></i></li>
      <li>Sneakers <i class="fa-solid fa-crown"></i></li>
      <li>Casual <i class="fa-solid fa-crown"></i></li>
    </ul>
    <ul class="brands-list" aria-hidden="true">
      <li>Nike <i class="fa-solid fa-crown"></i></li>
      <li>Adidas <i class="fa-solid fa-crown"></i></li>
      <li>Jordan <i class="fa-solid fa-crown"></i></li>
      <li>Puma <i class="fa-solid fa-crown"></i></li>
      <li>New Balance <i class="fa-solid fa-crown"></i></li>
      <li>Vans <i class="fa-solid fa-crown"></i></li>
      <li>Reebok <i class="fa-solid fa-crown"></i></li>
      <li>Converse <i class="fa-solid fa-crown"></i></li>
      <li>Limited <i class="fa-solid fa-crown"></i></li>
      <li>Edition <i class="fa-solid fa-crown"></i></li>
      <li>Sneakers <i class="fa-solid fa-crown"></i></li>
      <li>Casual <i class="fa-solid fa-crown"></i></li>
    </ul>
  </div>
</div>

<!-- ══════════════════════════════════════
     FOOTER
══════════════════════════════════════ -->
<footer class="footer">
  <div class="text">
    <a href="#" class="footer-logo">CROWNKICKS</a>
    <p>Discover the latest trends in footwear and elevate your style with our exclusive collection of sneakers, boots, and more.</p>
    <span>© 2026 CrownKicks. All rights reserved.</span>
  </div>
  <div>
    <span class="footer-col-title">Quick Links</span>
    <div class="footer-links">
      <a href="#">About Us</a>
      <a href="#">Contact</a>
      <a href="#">Terms & Conditions</a>
      <a href="#">Privacy Policy</a>
    </div>
  </div>
  <div>
    <span class="footer-col-title">Follow Us</span>
    <div class="social-media">
      <a href="#">Instagram</a>
      <a href="#">TikTok</a>
      <a href="#">Twitter / X</a>
      <a href="#">Facebook</a>
    </div>
  </div>
</footer>

<!-- ══════════════════════════════════════
     JAVASCRIPT
══════════════════════════════════════ -->
<script>
/* ═══════════════════════════════════════════
   CART STATE
═══════════════════════════════════════════ */
let cart = [];

/* ── helpers ── */
function updateCartCount() {
  const total = cart.reduce((s, i) => s + i.qty, 0);
  const el = document.getElementById('cart-count');
  el.textContent = total;
  el.classList.remove('pop');
  void el.offsetWidth;
  el.classList.add('pop');
}

function calcTotal() {
  return cart.reduce((s, i) => s + i.price * i.qty, 0);
}

/* ── render cart items ── */
function renderCart() {
  const container = document.getElementById('cart-items');
  const totalEl   = document.getElementById('cart-total-price');

  if (!cart.length) {
    container.innerHTML = `
      <div class="cart-empty">
        <i class="fa-solid fa-bag-shopping"></i>
        <p>Your cart is empty.<br>Add some kicks!</p>
      </div>`;
    totalEl.textContent = '$0';
    return;
  }

  container.innerHTML = cart.map(item => `
    <div class="cart-item" data-id="${item.id}">
      <img class="cart-item-img" src="${item.img}" alt="${item.name}">
      <div class="cart-item-details">
        <span class="cart-item-brand">${item.brand}</span>
        <span class="cart-item-name">${item.name}</span>
        <span class="cart-item-price">$${item.price * item.qty}</span>
        <div class="cart-item-controls">
          <button class="qty-btn" onclick="changeQty('${item.id}', -1)">−</button>
          <span class="qty-number">${item.qty}</span>
          <button class="qty-btn" onclick="changeQty('${item.id}', 1)">+</button>
        </div>
      </div>
      <button class="cart-item-remove" onclick="removeItem('${item.id}')" title="Remove">
        <i class="fa-solid fa-xmark"></i>
      </button>
    </div>`).join('');

  totalEl.textContent = `$${calcTotal()}`;
}

/* ── add to cart ── */
document.querySelectorAll('.add-to-cart').forEach(btn => {
  btn.addEventListener('click', e => {
    e.stopPropagation();
    const { name, brand, price, img } = btn.dataset;
    const id = name.toLowerCase().replace(/\s+/g, '-');
    const existing = cart.find(i => i.id === id);
    if (existing) {
      existing.qty++;
    } else {
      cart.push({ id, name, brand, price: Number(price), img, qty: 1 });
    }
    updateCartCount();
    renderCart();
    openCart();
  });
});

/* ── qty & remove ── */
function changeQty(id, delta) {
  const item = cart.find(i => i.id === id);
  if (!item) return;
  item.qty += delta;
  if (item.qty <= 0) cart = cart.filter(i => i.id !== id);
  updateCartCount();
  renderCart();
}
function removeItem(id) {
  cart = cart.filter(i => i.id !== id);
  updateCartCount();
  renderCart();
}

/* ── open / close ── */
function openCart() {
  document.getElementById('cart-sidebar').classList.add('open');
  document.getElementById('cart-overlay').classList.add('open');
  document.body.style.overflow = 'hidden';
}
function closeCart() {
  document.getElementById('cart-sidebar').classList.remove('open');
  document.getElementById('cart-overlay').classList.remove('open');
  document.body.style.overflow = '';
}

document.getElementById('cartIconWrapper').addEventListener('click', openCart);
document.getElementById('cart-close').addEventListener('click', closeCart);
document.getElementById('cart-overlay').addEventListener('click', closeCart);

/* init empty cart */
renderCart();
updateCartCount();

/* ═══════════════════════════════════════════
   HAMBURGER / MOBILE MENU
═══════════════════════════════════════════ */
const hamburger = document.getElementById('hamburger');
const navLinks  = document.getElementById('navbar-links');

hamburger.addEventListener('click', () => {
  hamburger.classList.toggle('open');
  navLinks.classList.toggle('open');
});

/* Close when a link is tapped on mobile */
navLinks.querySelectorAll('a').forEach(a => {
  a.addEventListener('click', () => {
    hamburger.classList.remove('open');
    navLinks.classList.remove('open');
  });
});

/* ═══════════════════════════════════════════
   QUICK CHIPS — filter visual only
═══════════════════════════════════════════ */
document.querySelectorAll('.qchip').forEach(chip => {
  chip.addEventListener('click', () => {
    document.querySelectorAll('.qchip').forEach(c => c.classList.remove('active'));
    chip.classList.add('active');
  });
});

/* ═══════════════════════════════════════════
   SEARCH — basic live highlight (placeholder)
═══════════════════════════════════════════ */
document.getElementById('searchInput').addEventListener('input', function () {
  const q = this.value.toLowerCase().trim();
  document.querySelectorAll('.product-card').forEach(card => {
    const name  = card.querySelector('.product-name').textContent.toLowerCase();
    const brand = card.querySelector('.product-brand').textContent.toLowerCase();
    card.style.opacity    = (!q || name.includes(q) || brand.includes(q)) ? '1' : '0.2';
    card.style.transform  = (!q || name.includes(q) || brand.includes(q)) ? '' : 'scale(0.96)';
    card.style.transition = 'opacity .3s, transform .3s';
  });
});

/* ═══════════════════════════════════════════
   NAVBAR SCROLL SHADOW
═══════════════════════════════════════════ */
window.addEventListener('scroll', () => {
  const nav = document.querySelector('.navbar');
  nav.style.boxShadow = window.scrollY > 10
    ? '0 4px 40px rgba(0,0,0,.6)'
    : 'none';
});

/* ═══════════════════════════════════════════
   SCROLL FADE-IN (Intersection Observer)
═══════════════════════════════════════════ */
const observer = new IntersectionObserver(entries => {
  entries.forEach(e => {
    if (e.isIntersecting) {
      e.target.style.opacity = '1';
      e.target.style.transform = 'translateY(0)';
      observer.unobserve(e.target);
    }
  });
}, { threshold: 0.1 });

document.querySelectorAll('.product-card, .featured-card, .card').forEach(el => {
  el.style.opacity    = '0';
  el.style.transform  = 'translateY(24px)';
  el.style.transition = 'opacity .5s ease, transform .55s ease';
  observer.observe(el);
});
</script>
</body>
</html>